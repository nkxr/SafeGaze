# SafeGaze 🚗💤
**Real-time Driver Monitoring System & Fatigue Detection**

SafeGaze คือโปรเจกต์ Computer Vision ที่พัฒนาขึ้นเพื่อตรวจสอบสถานะของผู้ขับขี่แบบ Real-time โดยมุ่งเน้นการป้องกันอุบัติเหตุจากการหลับใน (Drowsiness) และการละสายตาจากถนน (Distraction) ด้วยการวิเคราะห์พิกัดบนใบหน้า (Face Mesh) ผ่านกล้อง Webcam

![Python](https://img.shields.io/badge/Python-3.x-blue.svg)
![OpenCV](https://img.shields.io/badge/OpenCV-4.x-green.svg)
![MediaPipe](https://img.shields.io/badge/MediaPipe-Latest-orange.svg)

## ✨ ฟีเจอร์หลัก (Key Features)

* **Real-time Monitoring:** ตรวจจับใบหน้าและดวงตาด้วยความเร็วสูงและแม่นยำ
* **Smart Detection Logic:**
    * **Drowsiness:** คำนวณค่า EAR (Eye Aspect Ratio) เพื่อตรวจจับการหลับตา
    * **Yawning:** คำนวณค่า MAR (Mouth Aspect Ratio) เพื่อตรวจจับการหาว
    * **Distraction:** คำนวณ Head Pose (Pitch/Yaw) และ Iris Tracking เพื่อดูว่าผู้ขับขี่มองถนนหรือไม่
    * **Rapid Blink:** ตรวจจับการกระพริบตาที่ถี่ผิดปกติ (สัญญาณของความล้า)
* **Personalized Calibration:** ระบบเรียนรู้ค่าหน้าปกติของผู้ใช้ (3 วินาที) เพื่อลด False Alarm
* **Gamified HUD:** หน้าจอแสดงผลแบบเกม แสดงแถบพลังงานและคะแนนความเหนื่อย (Fatigue Score)
* **Sleep Lock Mechanism:** หากหลับนานเกินไป ระบบจะล็อคสถานะแจ้งเตือนจนกว่าผู้ใช้จะลืมตาค้างไว้ระยะหนึ่ง

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

* **Python:** ภาษาหลักในการพัฒนา
* **OpenCV:** จัดการภาพและวาดกราฟิก (UI)
* **MediaPipe:** โมเดล AI สำหรับจับจุดบนใบหน้า (Face Mesh & Iris)
* **NumPy:** คำนวณทางคณิตศาสตร์และเวกเตอร์

## ⚙️ การติดตั้ง (Installation)

1. **Clone โปรเจกต์**
   ```bash
   git clone [https://github.com/your-username/SafeGaze.git](https://github.com/your-username/SafeGaze.git)
   cd SafeGaze

2. **สร้าง Virtual Environment (แนะนำ)**

```bash
python -m venv .venv
# Windows
.venv\Scripts\activate
# Mac/Linux
source .venv/bin/activate

```


3. **ติดตั้ง Library ที่จำเป็น**
```bash
pip install -r requirements.txt

```



## 🚀 การใช้งาน (Usage)

รันคำสั่งเพื่อเริ่มโปรแกรม:

```bash
python src/main.py

```

### 🎮 การควบคุม (Controls)

* **`c`** : เริ่มการตั้งค่า (Calibration) *แนะนำให้ทำทุกครั้งก่อนเริ่มใช้งาน*
* **`m`** : เปิด/ปิด เส้น Face Mesh บนหน้า
* **`q`** : ออกจากโปรแกรม

## 📂 โครงสร้างโปรเจกต์ (Structure)

```text
SafeGaze/
├── detectors/          # โมเดลคำนวณค่าต่างๆ (ตา, ปาก, องศาหน้า)
├── logic/              # ระบบตัดสินใจ, จับเวลา, และการให้คะแนน
├── pipeline/           # การจัดการกล้องและ MediaPipe
├── src/                # ไฟล์ตั้งค่าและ Main Loop
├── ui/                 # ระบบวาดหน้าจอ (HUD & Overlay)
└── requirements.txt    # รายชื่อ Library

```

## ⚠️ ข้อควรระวัง

โปรเจกต์นี้เป็นระบบช่วยเหลือการขับขี่เบื้องต้น (Prototype) ไม่สามารถทดแทนความรับผิดชอบของผู้ขับขี่ได้ โปรดขับขี่ด้วยความระมัดระวังและพักผ่อนให้เพียงพอ
